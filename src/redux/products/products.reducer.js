import productData from './shop-items.data'

const INITIAL_STATE = {
    allProducts: productData
};

const productsReducer = (state = INITIAL_STATE) => {
    return state;
};

export default productsReducer;

